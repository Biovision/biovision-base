<h1><%= entity.title %></h1>

<% if entity.editable_by?(current_user) %>
  <ul class="actions">
    <li><%= edit_icon(edit_editable_page_path(id: entity.id)) %></li>
  </ul>
<% end %>

<div class="text">
  <%= raw(entity.body) %>
</div>
