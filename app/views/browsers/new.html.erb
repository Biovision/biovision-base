<% content_for :meta_title, t('.title') %>
<% content_for :breadcrumbs do %>
    <%= link_to(t('admin.browsers.nav_item.text'), admin_browsers_path) %>
    <span><%= t(:create) %></span>
<% end %>

<article>
  <h1><%= t('.title') %></h1>

  <ul class="actions">
    <li><%= back_icon(admin_browsers_path) %></li>
  </ul>

  <%= render partial: 'form', locals: { entity: @entity } %>
</article>
