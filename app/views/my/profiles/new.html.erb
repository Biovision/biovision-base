<% content_for :meta_title, t('.title') %>

<article class="profile-content my-profile-new">
  <div class="content-wrapper">
    <h1><%= t('.heading') %></h1>

    <%=
      render(
          partial: 'my/profiles/new/form',
          locals:  {
              entity:            @entity,
              component_handler: @handler
          }
      )
    %>
  </div>
</article>
