<% content_for :meta_title, t('.title', page: current_page) %>
<% content_for :hide_sidebar, '1' %>

<article class="entity-page">
  <h1><%= t('.heading') %></h1>
  <ul class="actions">
    <li><%= create_icon new_code_path %></li>
  </ul>

  <%= paginate @collection %>
  <%= render partial: 'list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
