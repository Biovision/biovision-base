<% content_for :meta_title, t('.title') %>
<% content_for :breadcrumbs do %>
    <span><%= t('admin.metrics.nav_item.text') %></span>
<% end %>

<article class="entity-page">
  <h1><%= t('.heading') %></h1>

  <ul class="actions">
    <li><%= back_icon(admin_path) %></li>
  </ul>

  <%= render partial: 'list', locals: { collection: @collection } %>
</article>
